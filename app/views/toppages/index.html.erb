<% if logged_in? %>
 <div class="container mb-5">
   <div class="row">
<div class="col-sm-12 mb-5">
      <%= render 'commons/articles', articles: @articles %>
     </div>
   </div>
      <%= render 'commons/footer' %>
  </div>
<% else %>
 <div class="center jumbotron">
    <div class="text-center">
      <h4>異なる文化や芸術を楽しむ。</h4>
        <%= link_to 'Sign up now!', new_user_path, class: 'btn btn-outline-primary mt-2' %>
     </div>
 </div>
   <%= render 'toppage' %>
<footer class="py-3  bg-light">
   <div class="container">
      <p class="text-right my-2">Copyright &copy 2020 IR</p>
   </div>
</footer>
<% end %>
